<main>
    <div class="container">
        <div class="jumbotron text-center my-3">
            <h3 style="font-weight: bold;">Your reservation is complete!</h3>
        </div>
        <div class="row">
            <div class="col-md-6 mx-auto"> 
                <div class="text-center">
                    <h4 class="my-3">Booking Details</h4>
                    {{!-- <h4 class="my-3">Parking Spot: {{reservation}}</h4>
                    <h4 class="my-3">Parking Spot: {{reservation.selectedParkingSpot}}</h4>
                    <h4 class="my-3">Parking Site: {{site}}</h4>
                    <h4 class="my-3">Parking Time: {{parkingTime}}</h4> --}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
</main>
